<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
        <a class="navbar-brand" href="/">Symfony CRUD</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('categories_index') }}">Categories</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('products_index') }}">Products</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">New</a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="{{ path('categories_new') }}">New Category</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="{{ path('products_new') }}">New Product</a>
                    </div>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Download PDF</a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="{{ path('products_pdf') }}">All Products</a>
                    </div>
                </li>
            </ul>
        </div>
    </div>
</nav>
